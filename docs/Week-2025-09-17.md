# Weekly Log — Week of 2025-09-17

## ⚙️ Overview
- Set up style tile (colors, type, basic logo) and exported PNG.
- Organized repo with branches and opened PRs (style tile → `dev`, tracking PR `dev → staging`).
- Started planning in GitHub (issues, labels, milestones, project board).
- Began Figma prototype (Home, Explore, Details, Saved, Filter Modal).

## 🌵 Challenges
- Figma quirks (couldn’t find “Duplicate”/Subtract); fixed by using “Save a copy” and Boolean ops from the toolbar.
- PR base/compare confusion; double-checked `base: dev ← compare: feature-...` and fixed wrong branches.
- Pushed to the wrong branch name once; corrected by pushing the actual local branch.
- Installed missing `prop-types` in the client app to resolve build error.

## 🏆 Accomplishments
- Style Tile exported and committed to `/docs/style`.
- Created/linked issues to Milestone and Project board.
- Opened and merged feature PRs (style/design assets) into `dev`.
- Prototype screens laid out in Figma; screenshot(s) exported for docs.

## 🔮 Next Steps
- Add basic routes (`/`, `/explore`, `/saved`) and wire the nav.
- Finish Filter Modal behavior (ESC/overlay close) and Saved empty state.
- Contrast/a11y pass on buttons, chips, and modal.
- Update research notes and weekly log; keep tracking PR updated with links.
